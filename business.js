/**
 *
 * @param {String} templateId Email template id should be needs to pass
 * @param {Object} data Data of the correspond email template id
 * @returns {html} for email
 */
const getEmailTemplate = async (templateId, data) => {};
